name: Build and Push to GHCR

on:
  push:
    branches:
      - main
      - develop
      - testing
  pull_request:
    branches:
      - main
      - develop
      - testing

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    strategy:
      matrix:
        include:
          - dockerfile: Dockerfile
            platform: linux/amd64,linux/arm64
            tag-suffix: ''
          - dockerfile: Dockerfile.raspberrypi
            platform: linux/arm64
            tag-suffix: '-rpi'
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./${{ matrix.dockerfile }}
          platforms: ${{ matrix.platform }}
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}${{ matrix.tag-suffix }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: |
            type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache${{ matrix.tag-suffix }}
            type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest${{ matrix.tag-suffix }}
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache${{ matrix.tag-suffix }},mode=max

# rpi-runtime-test:
#   name: Build (RPi) and run runtime ldd checks
#   runs-on: ubuntu-latest
#   needs: build-and-push
#   steps:
#     - name: Checkout repository
#       uses: actions/checkout@v4
#
#     - name: Set up QEMU
#       uses: docker/setup-qemu-action@v2
#
#     - name: Set up Docker Buildx
#       uses: docker/setup-buildx-action@v3
#
#     - name: Build RPi image and load locally
#       uses: docker/build-push-action@v5
#       with:
#         context: .
#         file: ./Dockerfile.raspberrypi
#         platforms: linux/arm64
#         load: true
#         tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:rpi-test
#
#     - name: Test runtime dependencies (ldd)
#       run: |
#         set -euo pipefail
#         echo "Running ldd checks inside rpi-test image"
#         docker run --rm --platform linux/arm64 --entrypoint sh ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:rpi-test -c "ldd /usr/bin/indiserver || true; ldd /usr/lib/libindidriver.so | grep 'not found' && (echo 'MISSING LIBS' && ldd /usr/lib/libindidriver.so) && exit 1 || echo 'OK - no missing libs'"
